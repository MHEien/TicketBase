{
  "name": "stripe-payment-plugin",
  "version": "2.0.0",
  "description": "Stripe payment gateway plugin for Events Platform using Plugin SDK Context-Aware approach",
  "main": "bundle.js",
  "scripts": {
    "dev": "echo 'For development, edit bundle.js directly and upload to MinIO storage'",
    "upload": "echo 'Upload bundle.js to your MinIO storage: aws s3 cp bundle.js s3://your-bucket/stripe-plugin/bundle.js'",
    "test": "echo 'Test the plugin by registering it in your admin app with the MinIO URL'"
  },
  "dependencies": {
    "@stripe/react-stripe-js": "^2.4.0",
    "@stripe/stripe-js": "^2.4.0",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.0"
  },
  "author": "Events Platform Team",
  "license": "MIT",
  "keywords": [
    "stripe",
    "payment",
    "plugin",
    "events",
    "tickets"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/your-repo/ticketsmonorepo"
  },
  "pluginMetadata": {
    "category": "payments",
    "extensionPoints": [
      "admin-settings",
      "payment-methods",
      "checkout-confirmation"
    ],
    "requiredPermissions": [
      "read:orders",
      "write:transactions"
    ],
    "priority": 100,
    "displayName": "Credit Card (Stripe)"
  }
}
